plugins:
  - jekyll-coffeescript
sass:
  syntax: scss
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
